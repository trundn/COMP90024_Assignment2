---
- hosts: allInstances
  vars_files:
    - host_vars/remote.yaml
  gather_facts: true

  roles:
    - role: instance-environment

- hosts: dbInstances
  vars_files:
    - host_vars/remote.yaml
  gather_facts: true

  roles: 
    - role: couchdb

- hosts: masterInstance
  vars_files:
    - host_vars/remote.yaml
  gather_facts: true

  roles: 
    - role: couchdb-environment

